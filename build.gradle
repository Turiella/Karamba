// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    id 'com.android.application' version '8.1.0' apply false // La versión puede variar
    // No necesitas el plugin de Kotlin aquí si tu proyecto es solo Java
}
